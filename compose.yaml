services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
  frontend:
    build: ./frontend
    environment:
      VITE_PORT: 8080
    ports:
      - "8080:8080"
    expose:
      - 8080